buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.0' // Or whatever version you're using
        classpath 'com.google.gms:google-services:4.3.3'  // ✅ Firebase Plugin moved inside
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
